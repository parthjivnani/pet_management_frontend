# Pet Management Frontend

A React frontend for the pet management app. Auth, pets, species, adoptions, and profile stuff.

## Setup

Install dependencies:

```bash
npm i
```

## Run

Start the dev server:

```bash
npm run dev
```

Then open the URL it prints in the terminal (usually `http://localhost:5173`).
